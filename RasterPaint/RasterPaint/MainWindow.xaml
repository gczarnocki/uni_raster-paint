<Window x:Class="RasterPaint.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RasterPaint"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525" Background="LightGray"
        MinHeight="350" MinWidth="525">
    
    <Window.Resources>
        <Style TargetType="Button" x:Key="ButtonStyle">
            <Setter Property="Width" Value="95"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Name="imageGrid" Grid.Row="1" SizeChanged="imageGrid_SizeChanged" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Image Name="myImage" VerticalAlignment="Top" HorizontalAlignment="Left"        
                MouseLeftButtonDown="myImage_MouseLeftButtonDown"
                MouseLeftButtonUp="myImage_MouseLeftButtonUp"/>
        </Grid>
        
        
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Name="drawingButton" Content="Draw Grid" Style="{StaticResource ButtonStyle}"
                     Click="DrawGrid"/>
            
            <Button Name="changeObjectColourButton" Content="Object Colour" Style="{StaticResource ButtonStyle}"/>
            
            <Button Name="changeBackgroundButton" Content="Backgr. Colour" Style="{StaticResource ButtonStyle}"/>
            
            <Button Name="removeObjectButton" Content="Remove object" Style="{StaticResource ButtonStyle}"
                    Click="removeObjectButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
